(function (app, Object) {

    'use strict';

    app.constant('LOG_OUTPUT_TYPES', Object.freeze({
        console: 'console',
        file: 'file'
    }));

})(window.govAuthenticator.app, window.Object);
